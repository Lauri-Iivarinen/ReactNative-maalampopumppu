export interface theme{
    light: any;
    dark: any;
}

export interface PumpCodes{
    name: string;
    code: string;
    value: any;
    valueType?: string;
}

export interface sahko {
    price: number;
    startDate: string;
    endDate: string;
}